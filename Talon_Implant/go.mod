module github.com/Cleck777/Talon_C2/Talon_Implant

go 1.22

require (
    // Your dependencies would be listed here, each with its own version.
    // For example:
    // github.com/some/dependency v1.2.3
)

// If you have any replace directives to point to local versions of dependencies or forks, they would be here.
// For example:
// replace github.com/old/dependency => ../path/to/local/dependency
